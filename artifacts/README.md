# Large artifacts

This tree is for local RunPod outputs only:

- `activations/`;
- `lens_outputs/`;
- `checkpoints/`.

The contents are ignored by Git. Every artifact should be traceable to a run
config, raw result file, code revision, and research-log entry.
