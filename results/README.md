# Results

`environment_report.json` and `artifact_preflight.json` appear here during
setup. Experimental runs should use immutable `run_<timestamp-or-id>/`
directories. Preserve raw data separately under `data/raw_outputs/` and never
overwrite a completed run.

