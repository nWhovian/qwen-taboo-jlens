# Loaded only after the project is trusted. On a remote Codex project this
# process runs on RunPod and connects to the private Jupyter server there.

[mcp_servers.jupyter]
command = "bash"
args = ["scripts/start_jupyter_mcp.sh"]
cwd = "."
startup_timeout_sec = 30
tool_timeout_sec = 1800
